<h2>PostItems index</h2>
<% if user_signed_in? %>
	<%= link_to "新規投稿する", new_post_item_path %>
<% end %>
	<table>
		<thead>
			<tr>
				<th></th>
				<th>Title</th>
				<th>Opinion</th>
				<th colspan="3"></th>
			</tr>
		</thead>

		<tbody>
			<% @post_items.each do |post_item| %>
			<tr>
				<td>
					<%#= link_to user_path(book.user) do %>
						<%#= attachment_image_tag(book.user, :profile_image, :fill, 50, 50, fallback: "no-image-mini.jpg") %>
					<%# end %>
				</td>
				<td><%= link_to post_item.name, post_item_path(post_item) %></td>
				<td></td>
			</tr>
			<% end %>
		</tbody>
	</table>

