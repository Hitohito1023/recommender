<div class="search-form">
  <%= form_with url: search_path, method: :get, local: true do |f| %>
    <i class="fas fa-search"></i>
    <%= f.text_field :content %>
    <%= f.select :model, options_for_select({ "User" => "user", "Item" => "PostItem" }) %>
    <%= f.submit '検索' %>
  <% end %>
</div>

