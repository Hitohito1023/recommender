<h1>ジャンル一覧</h1>

<div>
  <table>
    <th>
      <tr>
        <td>ジャンル名</td>
        <td></td>
      </tr>
    </th>
    <tbody>
      <% @genres.each do |genre| %>
      <tr>
        <td>
          <%= genre.name %>
        </td>
        <td>
          <%= link_to "Edit", edit_genre_path(genre) %>
        </td>
      </tr>
      <% end %>
    </tbody>
  </table>
</div>

<h3>新規作成</h3>
<%= link_to "ジャンル登録", new_genre_path %>