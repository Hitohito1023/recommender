<div class="search-user-content">
	<% users.each do |user| %>
		<div class="search-user">
			<%= link_to user_path(user) do %>
				<%= attachment_image_tag user, :profile_image, size:'250x250', fallback: "no_image_icon.jpg" %>
				<h3><%= user.name %></h3>
			<% end %>
		</div>
	<% end %>
</div>